# Environment variables for configuring the notes database (notes_backend)
# Copy this file to ".env" and adjust values as needed. The orchestrator will map these env vars.

# Supported providers: Sqlite (default)
DB_PROVIDER=Sqlite

# For Sqlite, a file-based connection string is typical:
DB_CONNECTION_STRING=Data Source=notes.db
